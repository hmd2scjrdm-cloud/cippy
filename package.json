{
  "name": "cippy-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); for (const f of ['index.html','styles.css','script.js','vercel.json']) require('fs').copyFileSync(f,'dist/'+f); require('fs').cpSync('assets','dist/assets',{recursive:true});\""
  }
}
